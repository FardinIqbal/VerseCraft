<!-- app/views/devise/sessions/new.html.erb -->

<div class="auth-page">
  <!-- Rotating Poems Section -->
  <%= render "shared/poems_rotation" %>

  <!-- Authentication Container -->
  <div class="auth-container">
    <h2 class="auth-title">Welcome Back to VerseCraft</h2>
    <p class="auth-subtitle">Please sign in to continue your literary journey.</p>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-group">
        <%= f.label :email, "Email Address", class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :password, "Password", class: "form-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
      </div>

      <%= f.submit "Sign In", class: "btn btn-primary btn-auth" %>
    <% end %>

    <p class="auth-footer">
      Don’t have an account? <%= link_to "Sign Up", new_user_registration_path, class: "auth-link" %>
    </p>

    <!-- Coming Soon Links for App Stores -->
    <div class="app-store-links">
      <p class="coming-soon">Coming Soon:</p>
      <div class="store-buttons">
        <a href="#" class="store-link" aria-label="Coming soon on Play Store">
          <i class="fab fa-google-play"></i>
          <span>Play Store</span>
        </a>
        <a href="#" class="store-link" aria-label="Coming soon on App Store">
          <i class="fab fa-apple"></i>
          <span>App Store</span>
        </a>
      </div>
    </div>
  </div>
</div>
